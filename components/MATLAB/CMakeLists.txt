cmake_minimum_required (VERSION 3.17)

if(NOT DEFINED SERAR_MATLAB_VERSION AND SERAR_WITH_HDF5)
    set(SERAR_MATLAB_VERSION "9.11")
endif()

set(MATLAB_ADDITIONAL_VERSIONS "2021b=9.11")
project("MATLAB.json")
